on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Qui est la
      run: whoami
      shell: pwsh
    - name: Run iristerm
      run: |
        C:\InterSystems\Cache\bin\iristerm.exe
      shell: Bash

